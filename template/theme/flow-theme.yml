# pdf-theme.yml 과 동일하되, 챕터(==)의 강제 페이지 나눔만 끈 변형.
# 페이지 나눔을 본문의 <<< 로 직접 제어하고 싶은 문서에서 opt-in 으로 사용한다.
#   :pdf-theme: flow        (-> flow-theme.yml)
# (테마 이름 X 는 파일 X-theme.yml 로 해석되므로, pdf-theme.yml 상속은 `extends: pdf`)
extends: pdf
heading:
  chapter:
    break-before: auto
